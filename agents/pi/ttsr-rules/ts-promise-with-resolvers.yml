id: ts-promise-with-resolvers
language: TypeScript
severity: warning
message: Use `Promise.withResolvers()` instead of `new Promise()` constructor
note: |
  `Promise.withResolvers()` keeps control flow linear and exposes
  typed resolver functions without callback nesting.
rule:
  pattern: "new Promise($$$)"
