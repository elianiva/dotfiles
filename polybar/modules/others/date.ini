[module/date]
type = internal/date
interval = 5

format = <label>
format-foreground = ${colors.foreground}
format-background = ${colors.background-alt}

label = %date%
label-padding = 1

date = %a, %d %B %Y
date-alt  = %H:%M %p
